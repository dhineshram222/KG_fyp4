I have mentioned complete playlist in below description box. Machine language means binary language generally is in 0101 format. Target code means machine code or binary language clear. Legzim means some collections of tokens. Syntax tree means in previous phase we generate a syntax tree as per the particular source program. Symbol table means whatever the legzins, whatever the tokens have generated, this all information have stored into the symbol table. hen type casting, improper arguments and operators in between data types. For example, you need to perform addition, that is a plus b and you can't mention plus operator is there. So this is also called as semantic error. Intermediate code means see here. This is an intermediate code. In code generation phase, they allocate the memory as per your program size. Optimize code means to optimize the lines in your program. Means less memory space and less time is required to execute the program.  Symbol table is the main data structure of the compiler. There are some columns like address, symbol, attributes and memory location. ID means identifier. 10 means constant. These are the memory location or address space of every variable. The symbol table is mainly known as the data structure of the compiler. It makes il very easy lo operate the searching and fetching process. Code Generator: When the memory is full or proper registers are not allocated. A compiler is a translator that converts the high-level language into themachine language. It reads the source program one character at_a time from Jefi to right and converts it intomeaningful lexernes. Code generation is the final stage of the compilation process. It takes the optimized intermediate code as input. The output of the program could run faster execution and take less memory space. It sclect and allocate memory space and registers to program contains.