A stack is a linear data structure in which insertions and deletions are allowed only at the end called the top of the stack. When we try to imagine a stack we can imagine it like stack of books or we could have a stack of coins. When we define a stack as an ADT we are only interested in knowing the stack operations from the user point of view. This simply means we are not interested in the implementation details at this moment. We are only trying to understand what type of operations can be performed on the stack. Now we will look into some primary and secondary operations which are supported in case of stack. First command is push one means simply push this element inside the stack. Then we have this pop operation which simply deletes the top most element from the stack which simply moves the topmost element outside the stack right. Then I'm asking you to check this is full that means a stack full. to tell you that this stack is capable of holding only three elements. A stack is a set of elements that can be added or removed at any time. The number of layers in a stack is called the size of the stack. The size of a stack can also be measured by the number of elements in it. A stack is a linear data structure in which insertions and deletions are allowed only at the end, called the tap of the stack. A stack of books with the words physics and mathematics written on it is a drawing of a stack of pancakes.