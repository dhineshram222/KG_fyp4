St Stack is an order of group of homogeneous items of element. Stack can either be fixed size or it may have sense of dynamic resizing. Mainly there are two types of stack like implicit stack and explicit stack. Every computer system has built in implicit stack. The implicit stack is used by the compiler to implement recursion. The next or second type of stack is explicit stack. Explicit stack is implemented by the programmers or developers. Plication self stack according to these marks, right? Hope so, each and every concept regarding stack will be clear. Explain the concept of Implicit & Explicit Stack & give its importance. Explain implementation of stack using ADT.  Stack is a linear data structure which follows a particular order in which the operations areperformed. Stack can either be a fixed size one or it may have a sense of dynamic resizing. There are two types of stack: Implicit and Explicit. xplicit Stack: PUSH & POP would be examples of explicate stack operations. Algorithms needing a stack can use this explicit stack. Explicit Stack is implemented by a programmers as ADT.