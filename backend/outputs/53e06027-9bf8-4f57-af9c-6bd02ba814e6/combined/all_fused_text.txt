============================================================
FULL VIDEO TRANSCRIPT
============================================================

Good morning everyone, I am Rishal Vanokar, Assistant Professor IT Department. So from in this video we will start our third unit of data structure and algorithm subject diets stack and queue. And the first topic in this unit is introduction of stack. So let's see what is mean by stack. Next. Before start the session, forming points are covered in this video which includes where this stack real life applications of stack, this representation types of stack that is implicit, explicit and also stack as an ADD, applications of stack in data structure and also some important questions which frequently are in SPPU exam pattern. So let's see one by one. The first point is where this stack. Next. Okay, so see here, stack is an abstract data types, is commonly used in most programming languages. What is member abstract data types? Abstract data types means stack use different types of operations and these operations or theory of that operations if you are done from the user. We can directly use push and pop operation while implementation. So it is an order of group of homogeneous items of element. Homogeneous items means similar types of items, right? Statue of linear data structure. What is member linear data structure? We already learned in my previous video. Limer data structure means all the data or elements are arranged in sequential manner or in linear fashion. So stack of linear data structure which follows a particular order in which the operations are performed. The order may be added in the first structure or first in last out both are same. Okay, so the order of stack is last in first out. Here this plate is we put last at the stack and we pop up this plate at the first, right? So elements are added to and remove from the top of the stack. The most recently added items are placed at the top of the stack, right? So we can implement stack by using array, by using structure, by using pointers and also by using linked list. Stack can either be fixed size or it may have sense of dynamic resizing. Stack how either fixed size or dynamic size as well. Clear? So this is just concept of stack and these are some examples like arrangement of plates, that are meant for books, coins and also storage of set of layers, right? So this is the concept of stack. In daily life we use this concept. Clear? Next. So next is real life applications of stack. The main real life applications of stack is like stack of cafeteria, coins, then a stack of shoe boxes, stack of nearly folded shirts, books, stones, coins etc. Also there is plates of tray, shipping in cargo, then a stack of drawers, railway yard etc. So these are all the real life applications of stack and we all are familiar with that, right? Next. So the following background repeats a stack as a representation. So see here this is just concept of diagram of stack. We can insert data one by one, this operation is called post operation and we can delete the data one by one. This operation is called pop operation. So the structure of stack is based on lasting first out. The items we post last, we can pop those items first, right? So this is a concept and structure of stack. Next. The next point is type soft stack. Mainly there are two types of stack like implicit stack and explicit stack. So let's see one by one, first type is implicit stack. What is implicit stack? So every computer system has built in implicit stack. It is not implemented by a program or developer. The implicit stack is used by the compiler to implement recursion. See, while implementation or while doing programming, you use these kind of functionalities like written address, we use local variables and also we return the particular value or parameters. So all these kinds of information are stored into the implicit stack. That's why implicit stack are built in or used by the compiler only, right? So when the control returns from function, the most recent allocation of this variable is fluid and previous set of variables become active, right? So simply implicit stack stored all the built in functions like written address, local variables, parameters, etc. All the built in values are stored by implicit stack. Clear? Next. And the next or second type of stack is explicit stack. Explicit stack is implemented by the programmers or developers. The algorithm's mating stack is used as explicit stack. See here, these are the examples of explicit stack. So push and pop operation. As per the algorithm, as per the requirements, we push the elements in the stack and also pop the elements from this stack. So push and pop, this will put the examples of explicit stack. And also, conversion of infix to prefix expression, recursive algorithm, traversive algorithm, this all kind of algorithms use explicit stack. So, what is the infix and also prefix expression, we will learn in the next video. Clear? So this is the concept of explicit stack, all the dynamic and all the operations, which is performed by the programmers, by the users. So at that time, we use explicit stack. Clear? Next. The next point is stack, I1, LED, or operations performed on stack. So we are not familiar with that operation. The first operation is push. Push means instead, elements 1 by 1 in the stack. Then pop, pop means removing elements from the stack. Then pick function. Pick function did that top that element from the stack without removing it. Right? The next one is full function. It's full function check that whether stack is full or empty. And is empty function check that if stack is empty. Clear? So this all functions are performed on the stack. Next. Okay. So next point is applications of stack in data structure, where we use stack in data structure. So the rather some applications, like convert infix expression to postfix and prefix expression, the evaluation of postfix expression, I will explain in the next video what is infix, postfix, and prefix. We also use stack in reverse string, also while checking parentheses, basis of subprograms or function calling, then parsing, parsing means to analyze the structure of computer programs. Then basic into binary conversion, then backtracking. So this all are applications of data structure, where we use stack, right? Next. So there is some important questions which frequently asked in SPPU exam pattern, right? What is stack? Explain implementation of stack unity, the concept of implicit explicit stack, recursion, and also some application self stack according to these marks, right? Hope so, each and every concept regarding stack will be clear. Next. So thank you and keep practicing. In my next video, we will learn about in detail operations of stack. Thank you.

============================================================
SLIDE CONTENTS (OCR Text + Diagrams)
============================================================

--- slide0_fused ---
Slide Content:
PUSH PoP

—— | | —
= ° Ms. Vrushali Dhanokar
== M.Tech CSE
== Assistant Professor

NULL IT Department
dhanokarvrushali@gmail.com

IT Department

Assistant Professor

M.Tech CSE

Ms. Vrushali Dhanokar

1.21 Introduction of Stack

Data Structure & Algorithms
UNIT 3: STACK & QUEUE

KOT

“eee |
pie
J

Diagram Description:
a block diagram showing the number of blocks in each block a block diagram showing the first layer of the system the logo of the indian air force a stack of ice creams with the words stacks on it a stack of plates with different colors on them a stack of wooden blocks a stack of books on top of each other books a stack of colorful books on a white background a stack of plates with different colors a person placing a coin into a pile of coins a stack of blue boxes with a white background an image of a blue plastic pipe a stack of books and stacks of coins a diagram showing the different types of data a block diagram with two blocks and one block a screenshot of a computer screen showing a number of different numbers a diagram showing the number of different parts in the system a diagram of a stack structure a block diagram showing the number of blocks in each block a block diagram showing the first layer of the system the logo of the indian air force

--- slide10_fused ---
Slide Content:
5. Gives applications of stack.

4. Explain the concept of Implicit & Explicit Stack & give its importance.

6M

3.

What is importance of stack in recursion? Explain with examples.

4M

2. Explain implementation of stack using ADT.

1. What is Stack?

By SPPU Exam Pattern:

Important Questions

Diagram Description:

--- slide11_fused ---
Slide Content:
Thank You...!

Diagram Description:

--- slide1_fused ---
Slide Content:
2 PND NF

Real Life Applications of Stack.

Stack Representations.

Types of Stack

Implicit Stack

Explicit Stack

Stack as an ADT/ Operations
Applications of Stack in Data Structure

Important Questions

First

8. Applications of Stack in Data Structure

7. Stack as an ADT/ Operations

6. Explicit Stack

5. Implicit Stack bast | STAC KS

4. Types of Stack

3. Stack Representations.

2. Real Life Applications of Stack.

1. What is Stack?

Course Outline

Diagram Description:
a stack of ice creams with the words stacks on it

--- slide2_fused ---
Slide Content:
* Stack can either be a fixed size one or it may have a sense of dynamic resizing.

¢ Astack can be implemented by means of Array, Structure, Pointer, and Linked List.

¢ Elements are added to and removed from the top of the stack (the most recently added items
are at the top of the stack).

* Stack is a linear data structure which follows a particular order in which the operations are
performed.

¢ The order may be LIFO(Last In First Out) or FILO(First In Last Out).

* It is an ordered group of homogeneous items of elements.

¢ Astack is an Abstract Data Type (ADT), commonly used in most programming languages.

What is Stack?

Diagram Description:
a stack of plates with different colors on them

--- slide3_fused ---
Slide Content:
* Stack can either be a fixed size one or it may have a sense of dynamic resizing.

¢ Astack can be implemented by means of Array, Structure, Pointer, and Linked List.

¢ Elements are added to and removed from the top of the stack (the most recently added items
are at the top of the stack).

¢ Stack is a linear data structure which follows a particular order in which the operations are
performed.

¢ The order may be LIFO(Last In First Out) or FILO(First In Last Out).

¢ Astack is an Abstract Data Type (ADT), commonly used in most programming languages.

o
* Itis an ordered group of homogeneous items of elements.

What is Stack?

Diagram Description:
a stack of wooden blocks a stack of books on top of each other books a stack of colorful books on a white background a stack of plates with different colors

--- slide4_fused ---
Slide Content:
e olack Of Draw

« Shunting of Tr
Railway Yard

S@TOUChla Uhays

A stack
of pennies

SHOE BOXES

A stack of
neatly folded shirts

» Shipment in a Cargo
« Plates on a tray

» Stack of Coins

» Stack of Drawers

= Shunting of Trains in

neatly folded shirts |

io « Stack of Drawers

Astack noel

of pennies _ Astack of 1 e Stack of Coins

Plates on a tray

Shipment in a Cargo

Cafeteria trays. - shoe boxes.

A stack of Astack of

Real Life Applications of Stack

Diagram Description:
a person placing a coin into a pile of coins a stack of blue boxes with a white background an image of a blue plastic pipe a stack of books and stacks of coins

--- slide5_fused ---
Slide Content:
* pata Element [ Osta Element |

[Data Element [ bata tomer |
[ beta Bement [ Oa Elmont |
| Data Element [ Pata Etoment |

Stack Stack

jue Beg

| | Pop

S

‘,

3

The following diagram depicts a stack and its operations —

Stack Representation

Diagram Description:
a diagram showing the different types of data

--- slide6_fused ---
Slide Content:
and the previous set of variables become active.

* When the control returns from function, The most recent allocation of these variables is freed

on top of the stack.

* Each time function is called, Return Address, Local Variables and Parameters are allocated

* This implicit stack used by the compiler to implement Recursion.

Se AAAI EL LORE

Type 1: Implicit Stack:
* Every computer system has built in implicit stack.

* It is not implemented by program developer.

ing main (} ‘
‘ 1

Function cat {7 Wunel

Fuca:

return 0;

Funcl{ }

function body:

Fune2t

function body;

* Every computer system has built in implicit stack.

Type 1: Implicit Stack:

return 0;

ENS Pe eee

Explicit Stack

1. Implicit Stack oe
void funci{ );

2. Explicit Stack rein proverree ta func

1. Implicit Stack

Funetion definition

There are two types of stack:

Types of Stack

Diagram Description:
a block diagram with two blocks and one block

--- slide7_fused ---
Slide Content:
4. Non-recursive tree traversal algorithm.

3. Non-recursive version of recursive algorithm.

Type 2: Explicit Stack:

¢ Explicit Stack is implemented by a programmers as ADT.

* Algorithms needing a stack can use this explicit stack.

Examples of Explicit Stack:
1. PUSH & POP would be examples of explicate stack operations.

2. Conversion of an expression from infix to prefix form.

1. PUSH & POP would be examples of explicate stack operations.

Examples of Explicit Stack:

* Algorithms needing a stack can use this explicit stack.

¢ Explicit Stack is implemented by a programmers as ADT.

Type 2: Explicit Stack:

Types of Stack Continue..

Diagram Description:
a screenshot of a computer screen showing a number of different numbers

--- slide8_fused ---
Slide Content:
| | | | DATA STRUCTURE

' |
ee a
Push SS

— pop()

5.

isEmptyQ : Check if stack is empty.

4. isFullQ : Check if stack is full.

3. peekQ : Get the top data element of the stack, without removing it.

2. popQ : Removing (accessing) an element from the stack.

1. pushQ : Pushing (storing) an element on the stack.

Stack a8 an ADT / Operations Performed on Stack

Diagram Description:
a diagram showing the number of different parts in the system a diagram of a stack structure

--- slide9_fused ---
Slide Content:
o
Se

“

fo

“

te

“

*

“

te

“

fe

“

eo
fe

fo

“

RS
fe

Convert infix expression to postfix and prefix
expressions

Evaluate the postfix expression

Reverse a string

Check well-formed (nested) parenthesis

Reverse a string

Process subprogram function calls

Parse (analyze the structure) of computer programs
Simulate recursion

In computations like decimal to binary conversion
In Backtracking algorithms (often used in
optimizations

and in games)

Applications of Stack in Data Structure

Diagram Description:
